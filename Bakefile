#!/bin/bash

use elixir
use phoenix

export ROOT=git
export AUTH_PASSWORD=pannekoek
export TOKEN=oompaloompadoompadeedo

setup() {
	super
	git init $ROOT
}

clean() {
	rm -rf $ROOT/**/*
	rm -rf $ROOT/.git
	git init $ROOT
}

seeds() {
  priv/scripts/seeds.sh
}

deploy() {
	lift deploy scry
}
